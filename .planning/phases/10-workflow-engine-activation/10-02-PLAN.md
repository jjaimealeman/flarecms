---
phase: 10-workflow-engine-activation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/plugins/core-plugins/workflow-plugin/routes.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/admin-routes.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-dashboard.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-content.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/templates/scheduled-content.ts
autonomous: true

must_haves:
  truths:
    - "Route files use the same Bindings/Variables types as every other route in the codebase"
    - "All 3 admin templates render inside the Catalyst layout theme"
    - "Template pages have consistent styling with other admin pages (sidebar, header, fonts)"
  artifacts:
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/routes.ts"
      provides: "API routes with correct type imports"
      contains: "import type { Bindings, Variables } from"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/admin-routes.ts"
      provides: "Admin routes with correct type imports"
      contains: "import type { Bindings, Variables } from"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-dashboard.ts"
      provides: "Dashboard template using Catalyst layout"
      contains: "renderAdminLayoutCatalyst"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-content.ts"
      provides: "Content detail template using Catalyst layout"
      contains: "renderAdminLayoutCatalyst"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/templates/scheduled-content.ts"
      provides: "Scheduled content template using Catalyst layout"
      contains: "renderAdminLayoutCatalyst"
  key_links:
    - from: "routes.ts"
      to: "packages/core/src/app.ts"
      via: "type import"
      pattern: "import type.*from.*app"
    - from: "templates/*.ts"
      to: "admin-layout-catalyst.template.ts"
      via: "renderAdminLayoutCatalyst import"
      pattern: "renderAdminLayoutCatalyst"
---

<objective>
Fix route type imports and migrate all 3 admin templates to the Catalyst layout theme.

Purpose: Routes currently define local Bindings/Variables types that conflict with app.ts (wrong KV binding name, missing variables), preventing clean route mounting. Templates use the legacy renderAdminLayout which renders outside the Catalyst sidebar/header theme used by all other admin pages since v1.15.0.

Output: 2 route files with correct type imports, 3 template files using renderAdminLayoutCatalyst.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-workflow-engine-activation/10-RESEARCH.md

Key reference files:
@packages/core/src/app.ts (Bindings/Variables type definitions)
@packages/core/src/templates/layouts/admin-layout-catalyst.template.ts (AdminLayoutCatalystData interface, renderAdminLayoutCatalyst function)
@packages/core/src/routes/admin-sync.ts (example route with correct type imports)
@packages/core/src/templates/pages/admin-content-list.template.ts (example template using Catalyst layout)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix route type imports in routes.ts and admin-routes.ts</name>
  <files>
    packages/core/src/plugins/core-plugins/workflow-plugin/routes.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/admin-routes.ts
  </files>
  <action>
    For BOTH files (routes.ts and admin-routes.ts):

    1. Delete the local `type Bindings = { ... }` block (lines ~6-15 in both files)
    2. Delete the local `type Variables = { ... }` block (lines ~17-25 in both files)
    3. Add import at the top: `import type { Bindings, Variables } from '../../../../app'`
       The relative path from `plugins/core-plugins/workflow-plugin/` to `app.ts` is `../../../../app`
    4. The Hono generic `new Hono<{ Bindings: Bindings; Variables: Variables }>()` stays the same -- only the type source changes
    5. Fix any TypeScript errors caused by the type change:
       - `c.get('user')` now returns the full Variables['user'] type which is optional (`user?: {...}`)
       - Add null checks: `const user = c.get('user'); if (!user) return c.json({ error: 'Unauthorized' }, 401)`
       - `c.env.KV` references must change to `c.env.CACHE_KV` (the actual binding name)
       - If any route accesses `c.env.KV`, replace with `c.env.CACHE_KV`
    6. Verify the createWorkflowRoutes() and createWorkflowAdminRoutes() functions are exported (they already are)
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | grep -i "workflow-plugin/routes\|workflow-plugin/admin-routes"` and confirm zero errors.
  </verify>
  <done>
    Both route files import Bindings/Variables from app.ts. No local type definitions remain. All env binding references match the actual wrangler.toml binding names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 3 templates from renderAdminLayout to renderAdminLayoutCatalyst</name>
  <files>
    packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-dashboard.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/templates/workflow-content.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/templates/scheduled-content.ts
  </files>
  <action>
    For ALL 3 template files, apply the same migration pattern:

    1. Replace the import:
       - OLD: `import { renderAdminLayout } from '@flare-cms/core/templates'`
       - NEW: `import { renderAdminLayoutCatalyst, type AdminLayoutCatalystData } from '../../../../templates/layouts/admin-layout-catalyst.template'`
       Use relative import (4 levels up from templates/ dir), NOT the package import, matching other admin template patterns.

    2. Replace the return statement at the bottom of each render function:
       - OLD pattern:
         ```
         return renderAdminLayout({
           title: '...',
           pageTitle: '...',
           content,
           user: data.user,
           currentPath: 'workflow'
         })
         ```
       - NEW pattern:
         ```
         const layoutData: AdminLayoutCatalystData = {
           title: '...',
           pageTitle: '...',
           currentPath: 'workflow',
           user: data.user ? {
             name: data.user.email || data.user.name || 'User',
             email: data.user.email || '',
             role: data.user.role || 'viewer'
           } : undefined,
           content
         }
         return renderAdminLayoutCatalyst(layoutData)
         ```

    3. Keep ALL existing HTML content (the `content` variable) unchanged. Only the layout wrapper changes.

    4. Specific currentPath values for sidebar highlighting:
       - workflow-dashboard.ts: `currentPath: 'workflow'`
       - workflow-content.ts: `currentPath: 'workflow'`
       - scheduled-content.ts: `currentPath: 'workflow'`

    5. The existing template content uses Tailwind classes -- these will work inside Catalyst layout. If any classes reference dark backgrounds that conflict (e.g., `bg-gray-900` on the outer wrapper), remove them since Catalyst provides its own background.

    6. Update the user property mapping in the data interfaces if needed -- Catalyst expects `{ name: string; email: string; role: string }` not the raw JWT user object.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | grep -i "workflow-plugin/templates"` and confirm zero errors.
  </verify>
  <done>
    All 3 templates use renderAdminLayoutCatalyst. No references to renderAdminLayout remain in the workflow plugin. Templates compile without errors.
  </done>
</task>

</tasks>

<verification>
Run full TypeScript check:
```bash
cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json
```
Confirm no errors from any file in `plugins/core-plugins/workflow-plugin/`.

Grep to confirm no legacy references remain:
```bash
grep -r "renderAdminLayout[^C]" packages/core/src/plugins/core-plugins/workflow-plugin/
# Should return nothing (renderAdminLayoutCatalyst is fine, renderAdminLayout alone is not)
grep -rn "type Bindings\|type Variables" packages/core/src/plugins/core-plugins/workflow-plugin/routes.ts packages/core/src/plugins/core-plugins/workflow-plugin/admin-routes.ts
# Should return nothing
```
</verification>

<success_criteria>
- Zero local Bindings/Variables type definitions in route files
- All 3 templates import and use renderAdminLayoutCatalyst
- Zero TypeScript compilation errors across all 5 modified files
- No references to renderAdminLayout (legacy) remain in the workflow plugin
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-engine-activation/10-02-SUMMARY.md`
</output>
