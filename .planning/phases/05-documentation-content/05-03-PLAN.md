---
phase: 05-documentation-content
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/cms/content/docs/admin/dashboard.md
  - packages/cms/content/docs/admin/content-management.md
  - packages/cms/content/docs/admin/collection-builder.md
  - packages/cms/content/docs/admin/media-library.md
  - packages/cms/content/docs/admin/plugins.md
  - packages/cms/content/docs/api-reference/rest-endpoints.md
  - packages/cms/content/docs/api-reference/filtering.md
  - packages/cms/content/docs/api-reference/authentication.md
  - packages/cms/content/docs/api-reference/api-tokens.md
autonomous: true

must_haves:
  truths:
    - "API Reference documents every public endpoint with method, path, auth requirement, request example, and response example"
    - "Admin docs explain dashboard, content CRUD, collection builder, media library, and plugin management"
    - "API filtering page has a WARNING callout about the known broken filter bug with client-side workaround"
  artifacts:
    - path: "packages/cms/content/docs/api-reference/rest-endpoints.md"
      provides: "Complete endpoint table with all 30+ endpoints"
      min_lines: 100
    - path: "packages/cms/content/docs/api-reference/authentication.md"
      provides: "JWT auth flow with login/register examples"
      min_lines: 60
    - path: "packages/cms/content/docs/admin/content-management.md"
      provides: "Content CRUD guide with editor types"
      min_lines: 50
  key_links:
    - from: "rest-endpoints.md"
      to: "authentication.md"
      via: "Cross-references to auth requirements"
      pattern: "JWT|authentication"
    - from: "api-tokens.md"
      to: "rest-endpoints.md"
      via: "Token usage in API calls"
      pattern: "X-API-Key|read-only"
---

<objective>
Author content for Admin (5 pages) and API Reference (4 pages) — the reference documentation sections.

Purpose: Admin docs help users navigate the CMS interface. API Reference is the most technical section — every endpoint documented with request/response examples like Stripe docs. The API filter bug must be documented honestly.

Output: 9 markdown files with frontmatter, ready for the seed script.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-content/05-RESEARCH.md
@.planning/phases/05-documentation-content/05-CONTEXT.md
@.planning/phases/05-documentation-content/05-01-SUMMARY.md

Source files to reference for accuracy:
@packages/core/src/routes/api.ts
@packages/core/src/routes/api-content-crud.ts
@packages/core/src/routes/api-media.ts
@packages/core/src/routes/api-system.ts
@packages/core/src/routes/auth.ts
@packages/core/src/routes/admin-api-tokens.ts
@packages/core/src/routes/admin-dashboard.ts
@packages/core/src/routes/admin-content.ts
@packages/core/src/routes/admin-collections.ts
@packages/core/src/routes/admin-media.ts
@packages/core/src/routes/admin-plugins.ts
@packages/core/src/middleware/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author API Reference section (4 pages)</name>
  <files>
    packages/cms/content/docs/api-reference/rest-endpoints.md
    packages/cms/content/docs/api-reference/filtering.md
    packages/cms/content/docs/api-reference/authentication.md
    packages/cms/content/docs/api-reference/api-tokens.md
  </files>
  <action>
    Write 4 markdown files with YAML frontmatter. Section: "api-reference". Tone: casual but precise like Stripe API docs.

    **rest-endpoints.md** (order: 1):
    - Complete endpoint table from the RESEARCH.md (30+ endpoints organized by category)
    - Categories: Content API, Media API, System API, Auth endpoints
    - For each endpoint: method, path, auth requirement (None/API Key/JWT), description
    - Show the response envelope format: `{ data, meta: { count, timestamp, cache, timing } }`
    - Include 3-4 detailed request/response examples for the most common operations:
      - GET /api/collections (list collections)
      - GET /api/collections/:collection/content (list content)
      - POST /api/content (create content)
      - DELETE /api/content/:id (delete content)
    - Each example must show the full curl command AND the TypeScript fetch equivalent
    - Read `packages/core/src/routes/api.ts` and `api-content-crud.ts` for actual route definitions

    **filtering.md** (order: 2):
    - Query parameters: limit, offset, status, collection, sort
    - Sort syntax: field name (ascending), -field (descending)
    - Advanced filtering syntax: `filter[field][operator]=value`
    - > [!WARNING] **Known issue:** Advanced filters (`filter[field][operator]=value`) are currently broken and return unreliable results. Use client-side filtering instead.
    - Show the client-side filtering workaround pattern:
      ```typescript
      // Fetch all items, then filter in your code
      const { data } = await fetch('/api/collections/blog-posts/content?limit=1000').then(r => r.json())
      const published = data.filter(item => item.data.status === 'published')
      ```
    - Pagination example with limit/offset

    **authentication.md** (order: 3):
    - JWT auth flow explained step by step
    - Login endpoint: POST /auth/login (form-encoded, NOT JSON!)
    - Registration endpoint: POST /auth/register
    - How the JWT cookie works (httpOnly cookie named `token`)
    - Auth check: GET /auth/me
    - Password change: POST /auth/change-password
    - Logout: GET/POST /auth/logout
    - Role-based access: admin, editor, author, viewer
    - Show complete login flow code example (TypeScript)
    - > [!NOTE] The auth endpoint uses `application/x-www-form-urlencoded`, not JSON. This is because it's an HTML form handler.
    - Read `packages/core/src/routes/auth.ts` for actual endpoint implementations

    **api-tokens.md** (order: 4):
    - What API tokens are (read-only access keys)
    - How to create them in the admin UI
    - Using tokens: `X-API-Key` header
    - Collection scoping (tokens can be limited to specific collections)
    - > [!NOTE] API tokens are read-only. You'll need user credentials (JWT) for write operations like creating or updating content.
    - Show usage example with fetch and curl
    - Read `packages/core/src/routes/admin-api-tokens.ts` and `packages/core/src/middleware/auth.ts` for actual implementation
  </action>
  <verify>
    - All 4 files exist in `packages/cms/content/docs/api-reference/`
    - `grep -c "\\[!WARNING\\]" packages/cms/content/docs/api-reference/filtering.md` shows at least 1 (known bug callout)
    - `grep "x-www-form-urlencoded" packages/cms/content/docs/api-reference/authentication.md` confirms form-encoded auth documented
    - rest-endpoints.md has comprehensive endpoint table
  </verify>
  <done>API Reference has 4 pages: complete endpoint table with examples, filtering with known bug callout, JWT auth flow, and API token usage. All endpoints from the source code are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Author Admin section (5 pages)</name>
  <files>
    packages/cms/content/docs/admin/dashboard.md
    packages/cms/content/docs/admin/content-management.md
    packages/cms/content/docs/admin/collection-builder.md
    packages/cms/content/docs/admin/media-library.md
    packages/cms/content/docs/admin/plugins.md
  </files>
  <action>
    Write 5 markdown files with YAML frontmatter. Section: "admin". Tone: casual, guide-style — "here's how to use this."

    **dashboard.md** (order: 1):
    - Overview of the admin interface at /admin
    - Quick stats (collections, content items, media files)
    - Navigation structure (sidebar menu)
    - First-time setup experience
    - Read `packages/core/src/routes/admin-dashboard.ts` for what the dashboard shows

    **content-management.md** (order: 2):
    - Creating, editing, deleting content
    - Editor types: Quill (rich text), MDX Editor (markdown), plain text fields
    - Status management (draft -> published)
    - Slug behavior (auto-generated, editable)
    - Content versioning and audit trail
    - Bulk operations if available
    - Read `packages/core/src/routes/admin-content.ts` for actual CRUD flow

    **collection-builder.md** (order: 3):
    - Creating collections via the admin UI
    - Adding fields (all available field types)
    - Schema preview
    - Managed vs dynamic collections (file-based vs UI-created)
    - Editing existing collection schemas
    - Read `packages/core/src/routes/admin-collections.ts` for capabilities

    **media-library.md** (order: 4):
    - Browsing uploaded media
    - Uploading files (drag & drop, single, multiple)
    - Virtual folder organization
    - Bulk operations (delete, move)
    - Metadata editing
    - Image dimensions and file info
    - Read `packages/core/src/routes/admin-media.ts` for features

    **plugins.md** (order: 5):
    - Plugin management in the admin UI
    - Activating/deactivating plugins
    - Available core plugins list
    - Plugin settings and configuration
    - Read `packages/core/src/routes/admin-plugins.ts` for management features
  </action>
  <verify>
    - All 5 files exist in `packages/cms/content/docs/admin/`
    - Each has valid YAML frontmatter with section: "admin"
    - Content management page mentions multiple editor types
    - Collection builder page references field types
  </verify>
  <done>Admin section has 5 pages covering dashboard, content management (with editor types), collection builder (with field types), media library (with bulk ops), and plugin management. All descriptions match actual admin UI capabilities.</done>
</task>

</tasks>

<verification>
- 9 markdown files exist across 2 section directories
- All frontmatter has required fields (title, slug, excerpt, section, order, status)
- API Reference rest-endpoints.md documents all 30+ endpoints
- Filtering page has WARNING callout for known bug
- Authentication page documents form-encoded login (not JSON)
- API tokens page notes read-only limitation
</verification>

<success_criteria>
- Every public API endpoint is documented with method, path, auth, and description
- API examples show both curl and TypeScript fetch patterns
- Known filter bug documented with WARNING callout and workaround
- Admin pages accurately describe the actual admin UI features
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-content/05-03-SUMMARY.md`
</output>
