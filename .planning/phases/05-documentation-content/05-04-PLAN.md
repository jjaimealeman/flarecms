---
phase: 05-documentation-content
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/cms/content/docs/plugins/plugin-system.md
  - packages/cms/content/docs/plugins/core-plugins.md
  - packages/cms/content/docs/plugins/building-plugins.md
  - packages/cms/content/docs/security/auth-system.md
  - packages/cms/content/docs/security/rate-limiting.md
  - packages/cms/content/docs/security/csrf-cors.md
  - packages/cms/content/docs/security/security-headers.md
  - packages/cms/content/docs/deployment/cloudflare-workers.md
  - packages/cms/content/docs/deployment/d1-database.md
  - packages/cms/content/docs/deployment/r2-storage.md
  - packages/cms/content/docs/deployment/wrangler-config.md
  - packages/cms/content/docs/deployment/ci-cd.md
autonomous: true

must_haves:
  truths:
    - "Plugin system docs explain hooks, lifecycle, and the PluginBuilder SDK with a complete custom plugin example"
    - "Security docs cover JWT auth internals, rate limiting config, CSRF/CORS, and all security headers"
    - "Deployment docs walk through Cloudflare Workers setup, D1 migrations, R2 config, wrangler.toml, and CI/CD pipeline"
  artifacts:
    - path: "packages/cms/content/docs/plugins/building-plugins.md"
      provides: "Complete guide to building a custom plugin with PluginBuilder"
      min_lines: 80
    - path: "packages/cms/content/docs/deployment/cloudflare-workers.md"
      provides: "Workers deployment guide"
      min_lines: 60
    - path: "packages/cms/content/docs/security/auth-system.md"
      provides: "JWT auth internals, password hashing, RBAC"
      min_lines: 60
  key_links:
    - from: "building-plugins.md"
      to: "plugin-system.md"
      via: "Hook types reference"
      pattern: "hook|lifecycle"
    - from: "cloudflare-workers.md"
      to: "wrangler-config.md"
      via: "Wrangler configuration reference"
      pattern: "wrangler\\.toml"
---

<objective>
Author content for Plugins (3 pages), Security (4 pages), and Deployment (5 pages) — the advanced documentation sections.

Purpose: These sections serve developers who are customizing, securing, and shipping FlareCMS to production. Plugin docs must show the PluginBuilder SDK. Security docs must be thorough. Deployment docs must be a complete Cloudflare Workers deployment guide.

Output: 12 markdown files with frontmatter, ready for the seed script.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-content/05-RESEARCH.md
@.planning/phases/05-documentation-content/05-CONTEXT.md
@.planning/phases/05-documentation-content/05-01-SUMMARY.md

Source files to reference for accuracy:
@packages/core/src/plugins/sdk/index.ts
@packages/core/src/plugins/hook-system.ts
@packages/core/src/plugins/types.ts
@packages/core/src/plugins/plugin-manager.ts
@packages/core/src/plugins/available/
@packages/core/src/middleware/auth.ts
@packages/core/src/middleware/rate-limit.ts
@packages/core/src/middleware/csrf.ts
@packages/core/src/middleware/security-headers.ts
@packages/cms/wrangler.toml
@.github/workflows/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Plugins section (3 pages) and Security section (4 pages)</name>
  <files>
    packages/cms/content/docs/plugins/plugin-system.md
    packages/cms/content/docs/plugins/core-plugins.md
    packages/cms/content/docs/plugins/building-plugins.md
    packages/cms/content/docs/security/auth-system.md
    packages/cms/content/docs/security/rate-limiting.md
    packages/cms/content/docs/security/csrf-cors.md
    packages/cms/content/docs/security/security-headers.md
  </files>
  <action>
    Write 7 markdown files with YAML frontmatter.

    **Plugins (section: "plugins"):**

    **plugin-system.md** (order: 1):
    - What the plugin system is and why it exists
    - Hook system overview — how plugins tap into the CMS lifecycle
    - Hook types: blocking vs non-blocking
    - List all available hook types (read from `packages/core/src/plugins/types.ts` and `hook-system.ts`)
    - Plugin lifecycle: registration -> activation -> hook execution
    - Read the actual hook system implementation for accuracy

    **core-plugins.md** (order: 2):
    - List all built-in plugins (read from `packages/core/src/plugins/available/`)
    - For each core plugin: name, what it does, configuration options
    - Expected plugins: Cache, Turnstile, Email, OTP Login, AI Search, Seed Data, Database Tools, etc.
    - Show how to activate/configure each one
    - Read the actual plugin files for accurate descriptions

    **building-plugins.md** (order: 3):
    - The PluginBuilder SDK (fluent API)
    - Complete example: building a custom plugin from scratch
    - Read `packages/core/src/plugins/sdk/index.ts` for the actual PluginBuilder API
    - Show: defining hooks, adding routes, adding middleware, adding admin pages
    - Plugin configuration and settings
    - Testing plugins
    - Make this a complete, self-contained tutorial — a developer should be able to build a working plugin by following this page

    **Security (section: "security"):**

    **auth-system.md** (order: 1):
    - JWT implementation details: HS256 algorithm, 24h expiry
    - Password hashing: PBKDF2 (or SHA-256 — check `packages/core/src/middleware/auth.ts` and the seed-admin.ts for actual implementation)
    - Role-based access control: admin, editor, author, viewer
    - Per-collection RBAC if implemented
    - Cookie-based JWT storage (httpOnly cookie named `token`)
    - Session management and token refresh behavior
    - Read the actual auth middleware for implementation details

    **rate-limiting.md** (order: 2):
    - KV-based sliding window implementation
    - Configuration options (read from `packages/core/src/middleware/rate-limit.ts`)
    - Per-route configuration if available
    - Default limits
    - How to customize rate limits
    - What happens when rate limited (response codes, headers)

    **csrf-cors.md** (order: 3):
    - CSRF token implementation (cookie-based)
    - Read `packages/core/src/middleware/csrf.ts` for actual implementation
    - CORS configuration via CORS_ORIGINS env var
    - How to configure for local development vs production
    - Multiple origin support (comma-separated)

    **security-headers.md** (order: 4):
    - All security headers set by the middleware
    - Read `packages/core/src/middleware/security-headers.ts` for the actual headers
    - X-Content-Type-Options, X-Frame-Options, HSTS, Referrer-Policy, Permissions-Policy
    - What each header does and why it matters
    - How to customize if needed

    **Accuracy requirement:** Read every source file listed above. Do NOT guess at hook types, plugin names, or security implementations. Use the actual code.
  </action>
  <verify>
    - All 7 files exist in their respective directories
    - Plugin system page lists actual hook types from source code
    - Building plugins page has complete PluginBuilder example
    - Auth system page correctly describes the hashing algorithm used (verify against source)
    - Security headers page lists actual headers from the middleware
  </verify>
  <done>Plugins section has 3 pages (system overview, core plugins catalog, building custom plugins tutorial). Security section has 4 pages (auth internals, rate limiting, CSRF/CORS, headers). All content reflects actual source code implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Author Deployment section (5 pages)</name>
  <files>
    packages/cms/content/docs/deployment/cloudflare-workers.md
    packages/cms/content/docs/deployment/d1-database.md
    packages/cms/content/docs/deployment/r2-storage.md
    packages/cms/content/docs/deployment/wrangler-config.md
    packages/cms/content/docs/deployment/ci-cd.md
  </files>
  <action>
    Write 5 markdown files with YAML frontmatter. Section: "deployment".

    **cloudflare-workers.md** (order: 1):
    - What Cloudflare Workers are and why FlareCMS uses them
    - Deploying with `wrangler deploy` (or `pnpm deploy:cms`)
    - Environment setup: development, staging, production
    - First deployment walkthrough
    - Custom domains
    - Read `packages/cms/wrangler.toml` for actual deployment config

    **d1-database.md** (order: 2):
    - What D1 is (SQLite on Cloudflare's edge)
    - Creating a D1 database: `wrangler d1 create`
    - Running migrations: how Drizzle ORM migrations work in FlareCMS
    - Local vs remote database
    - Backing up data
    - Common D1 gotchas (SQLite limitations)
    - Show the actual database_id from wrangler.toml as an example

    **r2-storage.md** (order: 3):
    - What R2 is (S3-compatible object storage)
    - Creating an R2 bucket: `wrangler r2 bucket create`
    - Custom domain setup (images.flarecms.dev)
    - How FlareCMS uses R2 for media
    - Local R2 behavior (wrangler dev uses local R2 — files stay local)
    - > [!NOTE] Local wrangler dev stores R2 files locally. Media uploaded during development won't be available in production. This is expected behavior.

    **wrangler-config.md** (order: 4):
    - Complete wrangler.toml reference for FlareCMS
    - Show the actual wrangler.toml from the project (or a cleaned-up version)
    - Explain each section: name, main, compatibility_date, compatibility_flags
    - D1 binding config, R2 binding config, KV binding config
    - Environment-specific overrides ([env.production])
    - Variables section ([vars])

    **ci-cd.md** (order: 5):
    - GitHub Actions workflow for FlareCMS
    - Read `.github/workflows/` for the actual workflow files
    - Build pipeline: build core -> deploy CMS (parallel) + build/deploy site (parallel)
    - Required GitHub secrets: CF_API_TOKEN, CF_ACCOUNT_ID
    - Setting up the workflow from scratch
    - Show the actual workflow YAML (or a representative version)
  </action>
  <verify>
    - All 5 files exist in `packages/cms/content/docs/deployment/`
    - wrangler-config.md references actual binding names from wrangler.toml (DB, MEDIA_BUCKET, CACHE_KV)
    - ci-cd.md references actual GitHub secrets (CF_API_TOKEN, CF_ACCOUNT_ID)
    - R2 page has NOTE callout about local R2 behavior
  </verify>
  <done>Deployment section has 5 pages: Workers deployment, D1 database setup, R2 storage, wrangler.toml reference, and CI/CD pipeline. All config values and commands reference the actual project setup.</done>
</task>

</tasks>

<verification>
- 12 markdown files exist across 3 section directories
- All frontmatter has required fields
- Plugin system page lists actual hook types from source
- Building plugins page has complete PluginBuilder SDK example
- Security pages reference actual middleware implementations
- Deployment pages use actual wrangler.toml values
- CI/CD page describes actual GitHub Actions workflow
</verification>

<success_criteria>
- Plugin docs include a complete custom plugin tutorial using PluginBuilder SDK
- Security docs cover all 4 middleware layers (auth, rate-limit, csrf, headers)
- Deployment docs are a complete guide from first deploy to CI/CD
- All code examples and config values are accurate against the actual codebase
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-content/05-04-SUMMARY.md`
</output>
