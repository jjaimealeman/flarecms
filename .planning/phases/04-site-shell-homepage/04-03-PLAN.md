---
phase: 04-site-shell-homepage
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/site/src/components/Hero.astro
  - packages/site/src/components/Features.astro
  - packages/site/src/components/ComparisonTable.astro
  - packages/site/src/components/OpenSourceCTA.astro
  - packages/site/src/data/homepage.ts
  - packages/site/src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Homepage shows hero section with version badge, tagline, CTA buttons, and terminal mockup"
    - "Homepage shows feature cards grid highlighting Flare CMS capabilities"
    - "Homepage shows comparison table (Flare CMS vs Traditional CMS) emphasizing Cloudflare-native advantages"
    - "Homepage shows bottom CTA section encouraging users to get started"
    - "Homepage is responsive -- all sections stack properly on mobile without horizontal overflow"
  artifacts:
    - path: "packages/site/src/components/Hero.astro"
      provides: "Redesigned hero with version badge, gradient headline, CTAs, terminal"
      min_lines: 40
    - path: "packages/site/src/components/Features.astro"
      provides: "Redesigned feature cards grid"
      min_lines: 30
    - path: "packages/site/src/components/ComparisonTable.astro"
      provides: "New Flare vs Traditional CMS comparison"
      min_lines: 40
    - path: "packages/site/src/components/OpenSourceCTA.astro"
      provides: "Redesigned bottom CTA"
      min_lines: 15
    - path: "packages/site/src/pages/index.astro"
      provides: "Updated homepage composition"
      min_lines: 10
  key_links:
    - from: "packages/site/src/pages/index.astro"
      to: "all homepage components"
      via: "component imports"
      pattern: "import.*(Hero|Features|ComparisonTable|OpenSourceCTA)"
    - from: "packages/site/src/components/Features.astro"
      to: "homepage.ts features data"
      via: "data import"
      pattern: "import.*features.*from.*homepage"
---

<objective>
Redesign the homepage per Stitch v2 mockups with hero section, feature cards, comparison table, and CTA.

Purpose: The current homepage has a good foundation (Hero, Stats, Features, CodeShowcase, BlogPreview, OpenSourceCTA) but needs to match the Stitch v2 mockup design with a dark navy/cyan/orange theme, comparison table replacing the stats section, and a more polished overall flow.

Output: Redesigned homepage with Hero (version badge + terminal), Features (card grid), ComparisonTable (new), and CTA sections. Responsive on all viewports.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-site-shell-homepage/04-CONTEXT.md
@.planning/phases/04-site-shell-homepage/04-RESEARCH.md

@packages/site/src/pages/index.astro
@packages/site/src/components/Hero.astro
@packages/site/src/components/Features.astro
@packages/site/src/components/Stats.astro
@packages/site/src/components/CodeShowcase.astro
@packages/site/src/components/OpenSourceCTA.astro
@packages/site/src/data/homepage.ts
@stitch-mockups-v2/screen_1.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Hero, Features data, and ComparisonTable</name>
  <files>
    packages/site/src/components/Hero.astro
    packages/site/src/components/ComparisonTable.astro
    packages/site/src/data/homepage.ts
  </files>
  <action>
    **Redesign Hero.astro** inspired by Stitch v2 mockup (reference screen_1.png):

    Keep the existing 2-column layout (copy left, terminal right) but enhance:
    - Version badge: keep existing pill badge with pulse dot
    - Headline: "Open-source headless CMS built on **Cloudflare Workers**" -- make "Cloudflare Workers" the gradient-highlighted text (from-cyan-400 to-flare-500), matching mockup style
    - Subheadline: keep similar messaging about edge deployment
    - Two CTA buttons: "Get Started" (primary, bg-flare-500) and "Documentation" (secondary, border outline)
    - Below CTAs: a CLI one-liner in a styled code block: `$ npx create-flare-app my-site` (matches mockup pattern). Style as a small inline terminal snippet with copy-on-click.
    - Terminal mockup on right: keep existing but ensure it's responsive. On mobile (`grid-cols-1`), terminal goes below the copy. Currently `grid-cols-2` with no responsive -- add `grid-cols-1 lg:grid-cols-2`.
    - Trust badges: keep existing row of tech badges below CTAs
    - Subtle grid background: keep existing pattern

    **Update homepage.ts features data:**
    Update the features array to better match Stitch v2 mockup's 4-card layout. Use 4 features (not 6):
    ```
    1. Edge-native Performance -- Runs on Cloudflare Workers across 300+ locations. Sub-50ms latency globally with zero cold starts.
    2. D1 + R2 + KV -- Cloudflare's native storage trifecta. D1 for database, R2 for media, KV for fast caching.
    3. Plugin Architecture -- Extend functionality with a robust plugin system. Custom fields, hooks, and middleware integration.
    4. Built-in Admin UI -- Manage content with an included admin dashboard. Rich text editing, media library, and user management out of the box.
    ```
    Update icons accordingly: globe, database, puzzle, layout-dashboard.

    Also add comparison data to homepage.ts:
    ```typescript
    export const comparisonRows = [
      { feature: 'Cold Starts', traditional: 'Common (500ms+)', flare: 'Zero', flareHighlight: true },
      { feature: 'Pricing', traditional: 'Expensive Tiered', flare: 'Pay-as-you-go', flareHighlight: true },
      { feature: 'Global Distribution', traditional: 'Region-based', flare: '275+ Locations', flareHighlight: true },
      { feature: 'Infrastructure', traditional: 'External Servers', flare: 'Cloudflare Native', flareHighlight: true },
      { feature: 'Database', traditional: 'External (Postgres/MySQL)', flare: 'D1 (Built-in)', flareHighlight: true },
      { feature: 'Media Storage', traditional: 'S3 / External', flare: 'R2 (Built-in)', flareHighlight: true },
    ]
    ```

    **Create ComparisonTable.astro:**
    New component showing "Why Flare CMS?" with comparison table.
    - Section title: "Why Flare CMS?" with subtitle "Compared to traditional headless CMS solutions"
    - Table with 3 columns: Feature, Traditional CMS, Flare CMS
    - Flare CMS column header styled with cyan-400 accent
    - Flare values in the table styled with `text-cyan-400 font-medium` when flareHighlight is true
    - Traditional values in muted `text-slate-500`
    - Table styled with slate-800 borders, dark background
    - RESPONSIVE: On mobile (below md), convert to a stacked card layout instead of table. Each row becomes a card showing Feature name, then Traditional vs Flare side by side. Use `hidden md:table` for the table and `md:hidden` for the card layout. This avoids horizontal overflow (Pitfall 5 from research).
  </action>
  <verify>
    Check Hero responsive grid: `grep "lg:grid-cols-2" packages/site/src/components/Hero.astro`
    Check ComparisonTable exists: `ls packages/site/src/components/ComparisonTable.astro`
    Check comparison data exists: `grep "comparisonRows" packages/site/src/data/homepage.ts`
    Check features reduced to 4: `grep -c "icon:" packages/site/src/data/homepage.ts` should show 4
  </verify>
  <done>Hero redesigned with responsive grid and CLI snippet. ComparisonTable created with responsive mobile layout. Features data updated to 4 cards. Comparison data added to homepage.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Features, CTA, and update index.astro</name>
  <files>
    packages/site/src/components/Features.astro
    packages/site/src/components/OpenSourceCTA.astro
    packages/site/src/pages/index.astro
  </files>
  <action>
    **Redesign Features.astro:**
    Update to show 4 feature cards in a 2x2 grid (matching mockup's layout):
    - `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` for responsive flow
    - Section label: small uppercase "FEATURES" text above the heading (text-xs text-cyan-400 uppercase tracking-widest)
    - Section heading: "Everything you need for the edge"
    - Each card: icon in a colored rounded square, title, description
    - Cards have subtle hover effect (existing pattern: `hover:bg-slate-800/60 hover:border-slate-700`)
    - Since we now have 4 features, the grid works perfectly at all breakpoints
    - Keep using inline SVGs for icons (existing pattern) -- update the icon set to match the 4 new features

    **Redesign OpenSourceCTA.astro:**
    Update the bottom CTA to match Stitch v2 mockup style:
    - Full-width section with gradient background: `bg-gradient-to-r from-cyan-900/50 to-flare-900/50` with a border top
    - Heading: "Ready to build on the edge?"
    - Subheading: "Join thousands of developers deploying faster with Flare CMS."
    - Single prominent CTA button: "Get Started Free" linking to /docs (primary flare-500 style)
    - Clean, simple layout -- no card wrapper, just a wide band

    **Update index.astro:**
    Update the homepage composition to the new section order matching Stitch v2 mockup flow:
    1. Hero (redesigned)
    2. Features (redesigned, 4 cards)
    3. CodeShowcase (keep as-is -- already shows schema + API demo)
    4. ComparisonTable (new)
    5. OpenSourceCTA (redesigned)

    Remove Stats and BlogPreview from the homepage. Stats is replaced by the comparison table. BlogPreview can return later but is not in the mockup design. Remove the Stats import entirely. Remove the BlogPreview import.

    Do NOT delete Stats.astro or BlogPreview.astro files -- they may be used elsewhere or reintroduced later. Just remove them from index.astro.
  </action>
  <verify>
    Check index.astro composition: `grep "import" packages/site/src/pages/index.astro`
    Verify Stats and BlogPreview removed from index: `grep -c "Stats\|BlogPreview" packages/site/src/pages/index.astro` should be 0
    Check ComparisonTable imported: `grep "ComparisonTable" packages/site/src/pages/index.astro`
    Check Features grid: `grep "lg:grid-cols-4" packages/site/src/components/Features.astro`
    Check CTA has gradient: `grep "gradient" packages/site/src/components/OpenSourceCTA.astro`
  </verify>
  <done>Features shows 4 cards in responsive grid. CTA redesigned with gradient band. Homepage composition updated: Hero, Features, CodeShowcase, ComparisonTable, CTA. Stats and BlogPreview removed from homepage.</done>
</task>

</tasks>

<verification>
1. Homepage loads with Hero -> Features -> CodeShowcase -> ComparisonTable -> CTA flow
2. Hero is responsive (stacks on mobile, 2-col on desktop)
3. Features shows 4 cards in responsive grid
4. ComparisonTable shows comparison data, responsive on mobile (cards instead of table)
5. CTA has gradient background with prominent button
6. No horizontal overflow on any viewport width
7. Stats.astro and BlogPreview.astro files still exist (not deleted), just removed from index.astro
</verification>

<success_criteria>
- Homepage matches Stitch v2 mockup flow: Hero, Features, Code, Comparison, CTA
- All sections responsive with no horizontal overflow on mobile
- Comparison table highlights Flare CMS advantages with cyan accent
- Design uses existing tokens: flare-500, cyan-400, slate-* scale
</success_criteria>

<output>
After completion, create `.planning/phases/04-site-shell-homepage/04-03-SUMMARY.md`
</output>
