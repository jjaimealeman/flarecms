---
phase: 02-docs-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/site/src/lib/flare.ts
  - packages/site/src/lib/docs-nav.ts
  - packages/site/src/layouts/DocsLayout.astro
  - packages/site/src/components/docs/DocsSidebar.astro
  - packages/site/src/components/docs/Breadcrumbs.astro
  - packages/site/src/components/docs/PrevNext.astro
autonomous: true

must_haves:
  truths:
    - "Docs sections and pages can be fetched from CMS API with published filter and order sort"
    - "Navigation tree groups docs pages under their parent section"
    - "DocsLayout renders a 3-column responsive grid wrapping Layout.astro"
    - "Sidebar shows all sections expanded with active page highlighting"
    - "Breadcrumbs show Docs > Section > Page path"
    - "Prev/next cards cross section boundaries based on order"
    - "Mobile menu button appears on small screens and toggles sidebar overlay"
  artifacts:
    - path: "packages/site/src/lib/flare.ts"
      provides: "getDocsSections() and getDocsPages() API functions with types"
      contains: "getDocsSections"
    - path: "packages/site/src/lib/docs-nav.ts"
      provides: "buildNavTree() and flattenForPrevNext() utilities with NavSection/NavPage types"
      exports: ["buildNavTree", "flattenForPrevNext", "NavSection", "NavPage"]
    - path: "packages/site/src/layouts/DocsLayout.astro"
      provides: "3-column responsive layout wrapping Layout.astro with sidebar, content slot, TOC placeholder"
      min_lines: 40
    - path: "packages/site/src/components/docs/DocsSidebar.astro"
      provides: "Left sidebar with section groups and active page pill highlight"
      min_lines: 20
    - path: "packages/site/src/components/docs/Breadcrumbs.astro"
      provides: "Breadcrumb trail with chevron separators"
      min_lines: 15
    - path: "packages/site/src/components/docs/PrevNext.astro"
      provides: "Prev/next navigation cards with hover effects"
      min_lines: 20
  key_links:
    - from: "packages/site/src/lib/flare.ts"
      to: "CMS API"
      via: "fetch to /api/collections/docs-sections/content and /api/collections/docs/content"
      pattern: "api/collections/docs"
    - from: "packages/site/src/lib/docs-nav.ts"
      to: "packages/site/src/lib/flare.ts"
      via: "uses DocsSection and DocsPage types"
      pattern: "import.*flare"
    - from: "packages/site/src/layouts/DocsLayout.astro"
      to: "packages/site/src/layouts/Layout.astro"
      via: "wraps Layout as outer shell"
      pattern: "import Layout"
---

<objective>
Create the docs data layer and all layout/navigation components for the 3-column docs layout.

Purpose: Build all the building blocks that docs routes will compose — API functions, navigation tree utilities, the responsive DocsLayout shell, sidebar, breadcrumbs, and prev/next components. These are all new files with no route wiring yet (Plan 02 handles routing).

Output: 6 files — extended flare.ts, new docs-nav.ts utility, DocsLayout.astro, DocsSidebar.astro, Breadcrumbs.astro, PrevNext.astro
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-docs-layout-navigation/02-RESEARCH.md
@.planning/phases/02-docs-layout-navigation/02-CONTEXT.md
@packages/site/src/lib/flare.ts
@packages/site/src/layouts/Layout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — API functions and nav tree builder</name>
  <files>
    packages/site/src/lib/flare.ts
    packages/site/src/lib/docs-nav.ts
  </files>
  <action>
    **flare.ts additions:**

    Add two interfaces and two fetch functions to the BOTTOM of the existing flare.ts file. Do NOT modify any existing code.

    1. Add `DocsSection` interface with fields: id, title, slug, status, data (containing name, slug, description?, icon?, color?, order?), created_at, updated_at

    2. Add `DocsPage` interface with fields: id, title, slug, status, data (containing title, slug, excerpt?, content, section (string — reference ID), order?, status?, lastUpdated?), created_at, updated_at

    3. Add `getDocsSections()` — fetches `/api/collections/docs-sections/content`, filters `status === 'published'`, sorts by `data.order ?? 0` ascending. Returns `DocsSection[]`.

    4. Add `getDocsPages()` — fetches `/api/collections/docs/content`, filters `status === 'published'`, sorts by `data.order ?? 0` ascending. Returns `DocsPage[]`.

    Follow the exact same pattern as existing `getBlogPosts()` / `getNewsArticles()` (fetch, check response.ok, parse JSON as FlareResponse, filter published, sort).

    **docs-nav.ts (new file):**

    Create `packages/site/src/lib/docs-nav.ts` with:

    1. Export `NavSection` interface: { name, slug, icon?, pages: NavPage[] }
    2. Export `NavPage` interface: { title, slug, href, sectionSlug, sectionName }
    3. Export `buildNavTree(sections: DocsSection[], docs: DocsPage[]): NavSection[]` — maps sections, for each section filters docs where `doc.data.section === section.id`, builds NavPage objects with `href: /docs/${sectionSlug}/${pageSlug}`. Uses `data.slug || slug` fallback pattern for both section and page slugs.
    4. Export `flattenForPrevNext(navSections: NavSection[]): NavPage[]` — flatMaps all pages from all sections into a single ordered array (order is already correct from sorted input).

    Import `DocsSection` and `DocsPage` types from `./flare` (use `import type`).
  </action>
  <verify>
    Run `npx tsc --noEmit --project packages/site/tsconfig.json 2>&1 | head -20` to check for type errors. If tsconfig doesn't support --noEmit, just verify the file syntax is valid by reading both files.
  </verify>
  <done>
    flare.ts has getDocsSections() and getDocsPages() functions with proper types. docs-nav.ts exports buildNavTree() and flattenForPrevNext() with NavSection/NavPage types. No type errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Layout and navigation components</name>
  <files>
    packages/site/src/layouts/DocsLayout.astro
    packages/site/src/components/docs/DocsSidebar.astro
    packages/site/src/components/docs/Breadcrumbs.astro
    packages/site/src/components/docs/PrevNext.astro
  </files>
  <action>
    Create the `packages/site/src/components/docs/` directory and all 4 files.

    **DocsSidebar.astro:**
    - Props: `sections: NavSection[]`, `currentPath: string`
    - Render all sections expanded (no accordion) in Astro docs style
    - Each section has a heading (text-xs, uppercase, tracking-wider, text-slate-400) with section name
    - Each page is a link with `class:list` conditional: active page gets `bg-slate-800 text-flare-400 font-medium rounded-md` pill style, inactive gets `text-slate-400 hover:text-slate-200 hover:bg-slate-800/50`
    - Compare `currentPath === page.href` for active detection
    - Use hardcoded Lucide icon map for section icons: map section slug to icon import (e.g., 'getting-started' -> `~icons/lucide/rocket`, 'core-concepts' -> `~icons/lucide/layers`, 'api-reference' -> `~icons/lucide/code`, 'admin' -> `~icons/lucide/settings`, 'security' -> `~icons/lucide/shield`, 'plugins' -> `~icons/lucide/puzzle`, 'deployment' -> `~icons/lucide/cloud`, 'configuration' -> `~icons/lucide/sliders`). Fall back to `~icons/lucide/book-open` for unknown slugs. Render icon inline next to section heading.
    - Import NavSection type from `../../lib/docs-nav`

    **Breadcrumbs.astro:**
    - Props: `items: Array<{ label: string; href: string }>`
    - Render an `<ol>` with flex layout, chevron separators between items (use `~icons/lucide/chevron-right`)
    - Last item is plain text (current page), earlier items are links
    - aria-label="Breadcrumb" on the nav element
    - Subtle text colors: links text-slate-500 hover:text-slate-300, current text-slate-300

    **PrevNext.astro:**
    - Props: `prev?: { title: string; section: string; href: string }`, `next?: { title: string; section: string; href: string }`
    - Grid with 2 columns, gap-4
    - Each card: rounded-lg border border-slate-800, hover:border-slate-700, hover:bg-slate-800/30 transition
    - Shows direction label ("Previous"/"Next") with arrow icon (`~icons/lucide/arrow-left` / `~icons/lucide/arrow-right`), page title (font-medium), section name (text-xs text-slate-500)
    - Prev card aligns left, next card aligns right (items-end, text-right)
    - Missing prev/next renders empty `<div />` to maintain grid positioning
    - Title hover color: text-flare-400 via group-hover
    - Wrapped in a border-t border-slate-800 mt-16 pt-8 nav element

    **DocsLayout.astro:**
    - Props: title, section ({ name, slug }), breadcrumbs, prev?, next?, sections (NavSection[]), currentPath
    - Import and wrap with `Layout` from `./Layout.astro` (relative import from layouts dir)
    - Import DocsSidebar, Breadcrumbs, PrevNext from `../components/docs/`
    - The layout grid uses responsive CSS Grid:
      - Base (mobile): `grid-cols-1` — sidebar hidden, TOC hidden, only content shows
      - md (tablet, 768px+): `grid-cols-[260px_1fr]` — sidebar visible, TOC still hidden
      - xl (desktop, 1280px+): `grid-cols-[260px_1fr_220px]` — all 3 columns
    - Container: `max-w-[1400px] mx-auto min-h-[calc(100vh-65px)]`
    - Sidebar column: `hidden md:block border-r border-slate-800 py-6 px-4 overflow-y-auto`
    - Content column: `px-6 md:px-10 py-8` with Breadcrumbs at top, then `<div class="prose prose-invert max-w-[65ch] mx-auto">` wrapping `<slot />`, then PrevNext below
    - TOC column: `hidden xl:block sticky top-[65px] h-[calc(100vh-65px)] overflow-y-auto py-8 px-4 border-l border-slate-800` with `<slot name="toc" />` (placeholder for Phase 3)
    - Mobile floating menu button: Only visible on screens below md breakpoint (`md:hidden`). Fixed position bottom-6 right-6, z-40. Circular bg-flare-500 button with menu icon (`~icons/lucide/menu`).
    - Mobile overlay: Fixed inset-0 z-50, initially hidden. Contains backdrop (bg-slate-950/80 backdrop-blur-sm) and slide-in nav panel (left-0, w-80, bg-slate-900, border-r border-slate-800). Nav panel renders the SAME DocsSidebar component with same props. Add a close button (X icon) at top-right of the panel.
    - Vanilla JS `<script is:inline>`: toggle hidden class on mobile overlay when button clicked, close when backdrop clicked, close when X clicked. Also close when any link inside the mobile nav is clicked (for navigation).
  </action>
  <verify>
    Verify all files exist and have correct imports:
    - `ls packages/site/src/components/docs/`
    - `ls packages/site/src/layouts/DocsLayout.astro`
    - Check that DocsLayout imports Layout, DocsSidebar, Breadcrumbs, PrevNext
    - Check that DocsSidebar imports NavSection type
  </verify>
  <done>
    All 4 component files created. DocsLayout wraps Layout.astro with 3-column responsive grid. DocsSidebar renders section groups with active page highlighting. Breadcrumbs renders clickable path with chevron separators. PrevNext renders prev/next cards with hover effects. Mobile menu button and overlay integrated into DocsLayout.
  </done>
</task>

</tasks>

<verification>
- All 6 files exist with correct content
- No TypeScript errors in the new code
- DocsLayout wraps Layout.astro (not duplicating header/footer)
- Responsive grid uses correct Tailwind breakpoints: 1-col (mobile), 2-col md (tablet), 3-col xl (desktop)
- Mobile menu button only shows below md breakpoint
- Sidebar uses Astro docs style with expanded sections and pill-shaped active state
</verification>

<success_criteria>
- getDocsSections() and getDocsPages() follow existing flare.ts patterns
- buildNavTree() joins docs to sections by matching doc.data.section === section.id
- DocsLayout provides 3-column grid that collapses responsively
- All components accept typed props and render correct HTML structure
- Mobile overlay includes vanilla JS toggle logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-docs-layout-navigation/02-01-SUMMARY.md`
</output>
