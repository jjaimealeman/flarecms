---
phase: 10-workflow-engine-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/workflow-service.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/content-workflow.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/automation.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/notifications.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/webhooks.ts
  - packages/core/src/plugins/core-plugins/workflow-plugin/services/scheduler.ts
autonomous: true

must_haves:
  truths:
    - "All 6 workflow service files compile without @ts-nocheck"
    - "WorkflowEngine.getAvailableTransitions queries role_permissions instead of non-existent user_permissions"
    - "WorkflowManager methods use workflow_history or logAudit instead of non-existent content_audit_log"
  artifacts:
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/workflow-service.ts"
      provides: "WorkflowEngine with fixed type safety and corrected SQL"
      contains: "role_permissions"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/content-workflow.ts"
      provides: "WorkflowManager with corrected audit logging"
      contains: "workflow_history"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/automation.ts"
      provides: "AutomationEngine with type safety"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/notifications.ts"
      provides: "NotificationService with type safety"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/webhooks.ts"
      provides: "WebhookService with type safety"
    - path: "packages/core/src/plugins/core-plugins/workflow-plugin/services/scheduler.ts"
      provides: "SchedulerService with type safety (production-active, behavior unchanged)"
  key_links:
    - from: "workflow-service.ts"
      to: "role_permissions table"
      via: "SQL query in getAvailableTransitions"
      pattern: "role_permissions"
    - from: "content-workflow.ts"
      to: "workflow_history table"
      via: "SQL INSERT/SELECT"
      pattern: "workflow_history"
---

<objective>
Fix all workflow plugin services to be type-safe and reference correct database tables.

Purpose: The 6 service files currently have @ts-nocheck directives hiding type errors, and 2 files reference database tables that do not exist (user_permissions, content_audit_log). These must be fixed before the plugin can be activated without runtime SQL errors.

Output: 6 type-safe service files that compile cleanly and reference only existing database tables.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-workflow-engine-activation/10-RESEARCH.md

Key reference files:
@packages/core/src/app.ts (Bindings and Variables types at lines 63-147)
@packages/core/src/services/audit-log.ts (logAudit service for reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix workflow-service.ts (SQL rewrite for role_permissions)</name>
  <files>
    packages/core/src/plugins/core-plugins/workflow-plugin/services/workflow-service.ts
  </files>
  <action>
    1. Remove `// @ts-nocheck` from line 1
    2. Fix `getAvailableTransitions()` method (~line 115): Replace the `user_permissions` subquery with a `role_permissions` + `permissions` join. The method signature should accept `userRole: string` instead of `userId: string` for the permission check. Pattern:
       ```sql
       SELECT wt.* FROM workflow_transitions wt
       WHERE wt.workflow_id = ? AND wt.from_state_id = ?
       AND (wt.required_permission IS NULL
            OR ? = 'admin'
            OR EXISTS (SELECT 1 FROM role_permissions rp
                       JOIN permissions p ON rp.permission_id = p.id
                       WHERE rp.role = ? AND p.name = wt.required_permission))
       ```
    3. Fix all TypeScript errors revealed by removing @ts-nocheck. Common issues will be:
       - `D1Database` type imports (add `/// <reference types="@cloudflare/workers-types" />` if needed)
       - `.first()` results needing null checks
       - `any` typed results from `.all()` needing type assertions
    4. Update any callers of `getAvailableTransitions` within this file to pass `userRole` instead of `userId`
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | grep -i "workflow-service"` and confirm zero errors from this file.
  </verify>
  <done>
    workflow-service.ts compiles without @ts-nocheck and queries role_permissions instead of user_permissions. Zero TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix content-workflow.ts (table reference fix)</name>
  <files>
    packages/core/src/plugins/core-plugins/workflow-plugin/services/content-workflow.ts
  </files>
  <action>
    1. This file does NOT have @ts-nocheck (it's the one clean service file)
    2. Replace ALL 3 references to `content_audit_log` table with `workflow_history` table. The `workflow_history` table already exists (created by migration 005) and has columns: id, workflow_id, content_id, from_state_id, to_state_id, transitioned_by, comment, created_at
    3. Adjust the INSERT statements to match workflow_history column names:
       - Map `action` -> `comment` (or build a descriptive comment from the action)
       - Map `from_status`/`to_status` -> `from_state_id`/`to_state_id`
       - Map `user_id` -> `transitioned_by`
       - Map `timestamp` -> `created_at`
       - Add `workflow_id` column (can be 'default' or fetched from content's assigned workflow)
    4. Fix the SELECT in `getWorkflowHistory()` to query `workflow_history` with correct column names
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | grep -i "content-workflow"` and confirm zero errors from this file.
  </verify>
  <done>
    content-workflow.ts references workflow_history instead of content_audit_log. All INSERT/SELECT statements use correct column names. Zero TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix automation.ts, notifications.ts, webhooks.ts, and scheduler.ts (remove @ts-nocheck)</name>
  <files>
    packages/core/src/plugins/core-plugins/workflow-plugin/services/automation.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/services/notifications.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/services/webhooks.ts
    packages/core/src/plugins/core-plugins/workflow-plugin/services/scheduler.ts
  </files>
  <action>
    For each of the 4 files:
    1. Remove `// @ts-nocheck` from line 1
    2. Fix all TypeScript errors revealed. Common patterns to fix:
       - Add `/// <reference types="@cloudflare/workers-types" />` if D1Database/KVNamespace types are unresolved
       - Add null checks after `.first()` calls (D1 returns `T | null`)
       - Add type assertions for `.all()` results: `const { results } = ... as { results: SomeType[] }`
       - Fix any `any` typed parameters that should have concrete types
       - Ensure constructor parameters are typed (typically `db: D1Database`)
    3. Do NOT change business logic or SQL queries -- only fix type errors
    4. If a file references tables that exist (notifications, webhook_configs, automation_rules -- all from migration 005), leave the SQL as-is
    5. **scheduler.ts is already active in production** -- be extra careful not to change any behavior. Only remove @ts-nocheck and fix type annotations.
  </action>
  <verify>
    Run `cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json 2>&1 | grep -i "automation\|notifications\|webhooks\|scheduler"` and confirm zero errors from these files.
  </verify>
  <done>
    All 6 service files (workflow-service, content-workflow, automation, notifications, webhooks, scheduler) compile without @ts-nocheck and have zero TypeScript errors. No business logic changed.
  </done>
</task>

</tasks>

<verification>
Run full TypeScript check on the core package:
```bash
cd /home/jaime/www/_github/flarecms && npx tsc --noEmit --project packages/core/tsconfig.json
```
Confirm no errors from any file in `plugins/core-plugins/workflow-plugin/services/`.

Run existing tests to ensure nothing is broken:
```bash
cd /home/jaime/www/_github/flarecms && pnpm test
```
</verification>

<success_criteria>
- All 6 service files have @ts-nocheck removed
- Zero TypeScript compilation errors in workflow plugin services
- user_permissions references replaced with role_permissions
- content_audit_log references replaced with workflow_history
- Existing test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-engine-activation/10-01-SUMMARY.md`
</output>
