---
phase: 05-documentation-content
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Seed script runs successfully and populates all 8 sections and 31 pages"
    - "All documentation is navigable from the sidebar with correct section grouping"
    - "Code examples render with syntax highlighting, callouts display correctly"
    - "Docs content can be wiped and re-seeded to return to a known state"
  artifacts: []
  key_links:
    - from: "packages/cms/scripts/seed-docs.ts"
      to: "CMS API"
      via: "HTTP POST requests with JWT auth"
      pattern: "seed.*complete|sections created|pages created"
---

<objective>
Run the seed script to populate all documentation content and verify everything renders correctly on the site.

Purpose: This is the integration test — proving that the seed script works end-to-end, all content appears in the sidebar, and pages render with proper formatting. Also verifies idempotency by running the seed script twice.

Output: All docs content live on localhost, verified rendering, idempotent seed confirmed.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-content/05-RESEARCH.md
@.planning/phases/05-documentation-content/05-01-SUMMARY.md
@.planning/phases/05-documentation-content/05-02-SUMMARY.md
@.planning/phases/05-documentation-content/05-03-SUMMARY.md
@.planning/phases/05-documentation-content/05-04-SUMMARY.md
@packages/cms/scripts/seed-docs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run seed script and verify API seeding</name>
  <files></files>
  <action>
    1. Ensure the CMS dev server is running at http://localhost:8787 (user runs this — just verify it's accessible with a curl health check: `curl http://localhost:8787/api/health`).

    2. Run the seed script:
       ```bash
       cd packages/cms && FLARE_ADMIN_PASSWORD=R3yK15VnqFTFFp npx tsx scripts/seed-docs.ts http://localhost:8787
       ```
       Note: The password is from seed-admin.ts (the dev admin account).

    3. Verify the output shows:
       - Successful authentication
       - 8 sections created
       - 31 pages created (3+4+3+4+5+3+4+5 = 31)
       - No errors

    4. Verify via API:
       - `curl http://localhost:8787/api/collections/docs-sections/content` returns 8 sections
       - `curl http://localhost:8787/api/collections/docs/content?limit=100` returns 31 pages
       - Spot-check a page: verify content field contains markdown, section field contains a UUID

    5. Test idempotency — run the seed script again:
       ```bash
       cd packages/cms && FLARE_ADMIN_PASSWORD=R3yK15VnqFTFFp npx tsx scripts/seed-docs.ts http://localhost:8787
       ```
       Verify it completes without errors and the counts remain the same (8 sections, 31 pages — not doubled).

    6. If any errors occur, fix the seed script or content files as needed. Common issues:
       - Auth failure: check password, check that CMS is running
       - FK constraint on delete: ensure pages are deleted before sections
       - Missing frontmatter field: fix the content .md file
  </action>
  <verify>
    - `curl -s http://localhost:8787/api/collections/docs-sections/content | python3 -c "import sys,json; print(json.load(sys.stdin)['meta']['count'])"` returns 8
    - `curl -s "http://localhost:8787/api/collections/docs/content?limit=100" | python3 -c "import sys,json; print(json.load(sys.stdin)['meta']['count'])"` returns 31
    - Running seed script twice produces same result (idempotent)
  </verify>
  <done>Seed script runs successfully, creates 8 sections and 31 pages, and is idempotent (re-running produces the same state).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 8 documentation sections seeded with 31 pages of substantive technical content</what-built>
  <how-to-verify>
    1. Visit http://localhost:4321/docs in your browser
    2. Verify the sidebar shows all 8 sections in order: Getting Started, Core Concepts, Configuration, Admin, API Reference, Plugins, Security, Deployment
    3. Click through each section — verify pages load with content
    4. Check Getting Started > Quickstart — verify it has actionable steps, code blocks with syntax highlighting
    5. Check API Reference > REST Endpoints — verify the endpoint table is complete and code examples render
    6. Check API Reference > Filtering — verify the WARNING callout about broken filters is visible and styled
    7. Check Core Concepts > Content Workflow — verify WARNING and CAUTION callouts for known bugs
    8. Check Plugins > Building Plugins — verify the PluginBuilder example has syntax highlighting
    9. Check any page with tab groups (Getting Started > Quickstart should have pnpm/npm/yarn tabs) — verify tabs switch correctly
    10. Spot-check 2-3 more pages of your choice for content quality and formatting
  </how-to-verify>
  <resume-signal>Type "approved" if docs look good, or describe any content/rendering issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Seed script completes without errors
- API confirms 8 sections and 31 pages exist
- Idempotency confirmed (double-run same result)
- Human verifies docs render correctly in browser
</verification>

<success_criteria>
- All documentation is seeded and navigable on localhost:4321/docs
- Sidebar shows 8 sections with correct page listings
- Code examples have syntax highlighting
- Callouts render with correct styling
- Tab groups function correctly
- Seed script is idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-content/05-05-SUMMARY.md`
</output>
