---
phase: 05-documentation-content
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/cms/content/docs/getting-started/quickstart.md
  - packages/cms/content/docs/getting-started/installation.md
  - packages/cms/content/docs/getting-started/project-structure.md
  - packages/cms/content/docs/core-concepts/architecture.md
  - packages/cms/content/docs/core-concepts/collections.md
  - packages/cms/content/docs/core-concepts/content-workflow.md
  - packages/cms/content/docs/core-concepts/media.md
  - packages/cms/content/docs/configuration/environment-variables.md
  - packages/cms/content/docs/configuration/bindings.md
  - packages/cms/content/docs/configuration/settings.md
autonomous: true

must_haves:
  truths:
    - "A new developer can follow the Getting Started quickstart from zero to a running local FlareCMS instance"
    - "Core Concepts pages explain architecture, collections, content workflow, and media with accurate code examples"
    - "Configuration pages provide a complete reference for env vars, bindings, and FlareConfig settings"
  artifacts:
    - path: "packages/cms/content/docs/getting-started/quickstart.md"
      provides: "5-minute quickstart guide"
      min_lines: 60
    - path: "packages/cms/content/docs/core-concepts/collections.md"
      provides: "Collection schema definition with all 34 field types"
      min_lines: 80
    - path: "packages/cms/content/docs/configuration/environment-variables.md"
      provides: "Complete env var reference"
      min_lines: 40
  key_links:
    - from: "quickstart.md"
      to: "installation.md"
      via: "Frontmatter order field (quickstart=1, installation=2)"
      pattern: "order: [12]"
    - from: "all content files"
      to: "seed-docs.ts"
      via: "Frontmatter format (title, slug, excerpt, section, order, status)"
      pattern: "^---"
---

<objective>
Author content for Getting Started (3 pages), Core Concepts (4 pages), and Configuration (3 pages) — the foundational documentation sections.

Purpose: These are the first sections a new developer reads. Getting Started must be actionable (clone-to-running in 5 min). Core Concepts must explain the architecture accurately. Configuration must be a complete reference. All code examples must reflect the actual FlareCMS codebase.

Output: 10 markdown files with frontmatter, ready for the seed script.
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-documentation-content/05-RESEARCH.md
@.planning/phases/05-documentation-content/05-CONTEXT.md
@.planning/phases/05-documentation-content/05-01-SUMMARY.md

Source files to reference for accuracy:
@packages/cms/wrangler.toml
@packages/cms/src/index.ts
@packages/cms/src/collections/docs.collection.ts
@packages/core/src/index.ts
@packages/core/src/types/index.ts
@packages/core/src/services/collection-service.ts
@packages/core/src/services/content-service.ts
@packages/core/src/services/media-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Getting Started section (3 pages)</name>
  <files>
    packages/cms/content/docs/getting-started/quickstart.md
    packages/cms/content/docs/getting-started/installation.md
    packages/cms/content/docs/getting-started/project-structure.md
  </files>
  <action>
    Write 3 markdown files with YAML frontmatter. Each file needs: title, slug, excerpt, section (= "getting-started"), order, status (= "published").

    **Tone:** Casual and friendly like Supabase/Remix docs. Use "you", be conversational. Concise — assume developer comfort with terminal/git/npm.

    **quickstart.md** (order: 1):
    - Prerequisites: Node.js 18+, pnpm, Wrangler CLI
    - Clone the repo, pnpm install, pnpm build
    - Run wrangler dev in packages/cms, visit localhost:8787/admin
    - Create your first content entry
    - Use tab groups for pnpm/npm/yarn where relevant
    - This should be followable in under 5 minutes
    - Read the actual `package.json` scripts and `wrangler.toml` to get commands right

    **installation.md** (order: 2):
    - Detailed prerequisites with version requirements
    - pnpm workspaces explanation (what the monorepo contains)
    - Cloudflare account setup (wrangler login)
    - D1 database creation, R2 bucket creation
    - Local development setup vs production setup
    - Environment variable configuration
    - Running migrations and seeding admin user
    - Troubleshooting common setup issues

    **project-structure.md** (order: 3):
    - Monorepo layout tree (packages/core, packages/cms, packages/site)
    - What each package does and its tech stack
    - Key files in each package (entry points, config files)
    - How packages depend on each other (core -> cms -> site)
    - Build order explanation

    Read the actual project structure to get file paths right. Use the repo's real directory layout.

    **Markdown features to use:**
    - Tab groups for package manager alternatives: ```bash tab="pnpm"
    - Callouts for important notes: > [!TIP], > [!NOTE], > [!WARNING]
    - Code blocks with language tags for syntax highlighting
    - Self-contained, copy-pasteable commands
  </action>
  <verify>
    - All 3 files exist in `packages/cms/content/docs/getting-started/`
    - Each has valid YAML frontmatter with all required fields
    - `grep -c "order:" packages/cms/content/docs/getting-started/*.md` shows order values 1, 2, 3
    - Code examples reference real commands from package.json
  </verify>
  <done>Getting Started has 3 substantive pages: quickstart (actionable 5-min guide), installation (detailed setup), project-structure (monorepo overview). All code examples reflect actual FlareCMS commands.</done>
</task>

<task type="auto">
  <name>Task 2: Author Core Concepts (4 pages) and Configuration (3 pages) sections</name>
  <files>
    packages/cms/content/docs/core-concepts/architecture.md
    packages/cms/content/docs/core-concepts/collections.md
    packages/cms/content/docs/core-concepts/content-workflow.md
    packages/cms/content/docs/core-concepts/media.md
    packages/cms/content/docs/configuration/environment-variables.md
    packages/cms/content/docs/configuration/bindings.md
    packages/cms/content/docs/configuration/settings.md
  </files>
  <action>
    Write 7 markdown files with YAML frontmatter. Same format as Task 1.

    **Core Concepts (section: "core-concepts"):**

    **architecture.md** (order: 1):
    - Edge-first design philosophy (Cloudflare Workers, no Node.js server)
    - Hono web framework for routing
    - D1 (SQLite) for data, R2 for media, KV for caching
    - Request lifecycle: middleware chain -> route handler -> response
    - Drizzle ORM for type-safe database access
    - Read `packages/core/src/index.ts` and `packages/cms/src/index.ts` for the actual app creation pattern

    **collections.md** (order: 2):
    - What collections are (typed content schemas)
    - CollectionConfig interface with a real example from the codebase
    - All field types available (read from `packages/core/src/types/index.ts` — the FieldType union has ~34 types)
    - Schema definition format (JSON Schema-like with properties, required)
    - listFields, searchFields, defaultSort configuration
    - Collection auto-sync (how file-based collections sync to DB)
    - Show a complete collection definition example
    - > [!NOTE] callout about select field default being ignored (known bug)

    **content-workflow.md** (order: 3):
    - Content lifecycle: draft -> published -> archived
    - Slug generation and locking behavior
    - Content versioning (if implemented — check source)
    - Soft delete behavior
    - > [!WARNING] callout: status is one-way, can't unpublish (known bug)
    - > [!CAUTION] callout: soft-delete doesn't cascade (known bug)

    **media.md** (order: 4):
    - R2 storage integration
    - Upload pipeline (single file, multiple files)
    - Image dimension extraction
    - Virtual folders
    - Custom domain (images.flarecms.dev)
    - Media API endpoints overview

    **Configuration (section: "configuration"):**

    **environment-variables.md** (order: 1):
    - Complete env var reference table: name, type, default, description
    - Include: ENVIRONMENT, CORS_ORIGINS, MEDIA_DOMAIN, WEBHOOK_URLS, WEBHOOK_SECRET, JWT_SECRET
    - Read `wrangler.toml` for actual var names and defaults
    - Read source code for any env vars accessed via `c.env` or `env.` patterns

    **bindings.md** (order: 2):
    - D1 database binding (DB) — what it is, how to configure
    - R2 bucket binding (MEDIA_BUCKET) — media storage
    - KV namespace binding (CACHE_KV) — rate limiting cache
    - Show the actual wrangler.toml binding config blocks
    - Explain local vs production binding differences

    **settings.md** (order: 3):
    - FlareConfig / createFlareApp configuration object
    - Available options (read from types)
    - Collection autoSync behavior
    - Plugin directory configuration
    - Middleware configuration options

    **Accuracy requirement:** Read the actual source files referenced in the context section. Do NOT guess at API surfaces — use real code. Use `grep` or read files to verify field types, function signatures, and configuration options.

    **Known bugs to document:**
    - Select field `default` ignored -> NOTE callout in collections.md
    - Status is one-way -> WARNING callout in content-workflow.md
    - Soft-delete doesn't cascade -> CAUTION callout in content-workflow.md
  </action>
  <verify>
    - All 7 files exist in their respective directories
    - Each has valid YAML frontmatter
    - `grep -l "\\[!WARNING\\]" packages/cms/content/docs/core-concepts/content-workflow.md` finds the known bug callouts
    - `grep -l "FieldType\\|field type" packages/cms/content/docs/core-concepts/collections.md` confirms field types documented
    - Configuration files reference actual wrangler.toml values
  </verify>
  <done>Core Concepts has 4 pages covering architecture, collections (with all field types), content workflow (with known bug callouts), and media. Configuration has 3 pages with complete env var reference, bindings guide, and settings reference. All content is accurate against the actual codebase.</done>
</task>

</tasks>

<verification>
- 10 markdown files exist across 3 section directories
- All frontmatter has required fields (title, slug, excerpt, section, order, status)
- Known bugs documented with appropriate callout types
- Code examples reference real FlareCMS patterns from source code
- Tab groups used for package manager alternatives in Getting Started
</verification>

<success_criteria>
- Getting Started quickstart is followable from zero to running instance
- Collections page documents all field types from the FieldType union
- Configuration pages are complete references (not just overviews)
- All 4 known bugs are documented honestly with callouts
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-content/05-02-SUMMARY.md`
</output>
