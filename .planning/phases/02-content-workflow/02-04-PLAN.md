---
phase: 02-content-workflow
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - sonicjs-fork/packages/core/src/templates/components/version-history.template.ts
  - sonicjs-fork/packages/core/src/routes/admin-content.ts
autonomous: true

must_haves:
  truths:
    - "Version history modal opens and closes correctly without page reload"
    - "Rollback to a previous version restores the expected content state"
    - "Rollback creates a workflow_history entry with action_type='rollback'"
  artifacts:
    - path: "sonicjs-fork/packages/core/src/templates/components/version-history.template.ts"
      provides: "Working version history modal with proper HTMX attributes"
    - path: "sonicjs-fork/packages/core/src/routes/admin-content.ts"
      provides: "Version rollback route handler"
  key_links:
    - from: "sonicjs-fork/packages/core/src/templates/components/version-history.template.ts"
      to: "sonicjs-fork/packages/core/src/routes/admin-content.ts"
      via: "HTMX hx-post to rollback endpoint"
      pattern: "hx-post.*version|rollback"
---

<objective>
Fix content versioning modal close bug (#666) and ensure rollback produces expected content state.

Purpose: CONT-04 — The version history modal has a close behavior bug and rollback stability needs verification. This is a targeted UI/UX fix in the HTMX admin.
Output: Working version history modal with stable rollback
</objective>

<execution_context>
@/home/jaime/.claude/get-shit-done/workflows/execute-plan.md
@/home/jaime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-content-workflow/02-RESEARCH.md (Pattern 7: Content Versioning Modal Fix)
@.planning/phases/02-content-workflow/02-02-SUMMARY.md (state machine from Plan 02)
@.planning/phases/02-content-workflow/02-03-SUMMARY.md (audit trail service from Plan 03)
@sonicjs-fork/packages/core/src/templates/components/version-history.template.ts
@sonicjs-fork/packages/core/src/routes/admin-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix version history modal close bug</name>
  <files>
    sonicjs-fork/packages/core/src/templates/components/version-history.template.ts
    sonicjs-fork/packages/core/src/routes/admin-content.ts
  </files>
  <action>
  This task requires investigation before fixing. The exact bug is unknown per the research doc.

  **Step 1: Read and understand the template**
  Read `version-history.template.ts` completely. Look for:
  - How the modal is opened (HTMX trigger, button click)
  - How the modal is closed (close button, overlay click, `hx-swap` behavior)
  - The HTMX attributes: `hx-get`, `hx-target`, `hx-swap`, `hx-trigger`
  - Whether `hx-swap-oob` is used (out-of-band swaps can cause close issues)

  **Step 2: Read the admin-content.ts route handlers for version history**
  Find the route that renders the version history (likely a GET endpoint returning HTML fragment). Also find the rollback POST handler if it exists.

  **Step 3: Identify the bug**
  Common HTMX modal close issues:
  - Missing `hx-swap="innerHTML"` causing the entire page to be replaced
  - Modal overlay not having a close handler (click-outside-to-close)
  - `hx-target` pointing to wrong element after swap
  - Missing `HX-Trigger: closeModal` response header for coordinated close
  - Modal content being swapped into the wrong container

  **Step 4: Fix the modal**
  Apply the fix based on investigation. Ensure:
  - Modal opens in an overlay/dialog
  - Close button triggers modal removal (not page navigation)
  - Clicking outside the modal closes it
  - After rollback action, modal closes and the underlying content form refreshes

  **Step 5: Fix/verify rollback handler**
  The rollback should:
  1. Read the selected version from `content_versions` table
  2. Update `content.data` with the version's `data` column
  3. Update `content.updated_at`
  4. Insert a `workflow_history` entry with `action_type = 'rollback'` and `comment` noting which version was restored — use the audit-trail service from Plan 03 (import `logAuditEvent` from `services/audit-trail.ts`)
  5. Return the updated content form (or trigger a page refresh via HTMX)

  If the rollback handler doesn't exist, create it at POST `/admin/content/:id/rollback/:versionId`.

  After changes: `cd /home/jaime/www/_github/sonicjs/sonicjs-fork && pnpm build && cd /home/jaime/www/_github/sonicjs/my-astro-cms && pnpm install`
  </action>
  <verify>
  1. Build succeeds: `cd sonicjs-fork && pnpm build`
  2. Automated check — confirm HTMX modal attributes exist in the template:
     `grep -E 'hx-(get|post|target|swap|trigger)' sonicjs-fork/packages/core/src/templates/components/version-history.template.ts`
     Should show at least 3 HTMX attribute matches (open, close, rollback triggers).
  3. Automated check — confirm rollback route exists:
     `grep -E 'rollback|action_type.*rollback' sonicjs-fork/packages/core/src/routes/admin-content.ts`
  4. Manual verification needed: Open admin UI, navigate to a content item with version history, click version history button, verify modal opens, verify close works, verify rollback works. (Dev server should be running at http://localhost:8787/admin)
  </verify>
  <done>
  - Version history modal opens as an overlay
  - Modal close button works without page reload
  - Clicking outside modal closes it
  - Rollback endpoint exists and restores content from version record
  - Rollback creates audit trail entry via audit-trail service (Plan 03 dependency satisfied)
  </done>
</task>

</tasks>

<verification>
1. Fork builds without errors
2. `grep -E 'hx-(get|post|target|swap|trigger)' sonicjs-fork/packages/core/src/templates/components/version-history.template.ts` returns 3+ matches
3. `grep -E 'rollback' sonicjs-fork/packages/core/src/routes/admin-content.ts` confirms rollback handler exists
4. Version history modal opens/closes correctly in admin UI
5. Rollback restores expected content state
6. No JavaScript console errors during modal interaction
</verification>

<success_criteria>
The content versioning modal closes correctly (bug #666 resolved) and rollback to a previous version produces the expected content state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-workflow/02-04-SUMMARY.md`
</output>
